# FinSight AI - Configuration Template
# Copiez ce fichier vers .env et remplissez vos clés API
# Pour générer une SECRET_KEY aléatoire, exécutez: py -c "import secrets; print(secrets.token_hex(32))"

# Flask Secret Key (OBLIGATOIRE pour les sessions Flask)
# Générez une clé aléatoire avec: py -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=dev-secret-key-change-in-production-please-generate-a-random-key

# Google Gemini API Key (gratuit - obtenez-le sur https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=votre_clé_gemini_ici

# Alpha Vantage API Key (gratuit - obtenez-le sur https://www.alphavantage.co/support/#api-key)
ALPHA_VANTAGE_API_KEY=votre_clé_alpha_vantage_ici

# Modèle LLM (optionnel, par défaut: gemini-2.0-flash-exp)
# Options: gemini-2.0-flash-exp, gemini-1.5-pro, gemini-1.5-flash
LLM_MODEL=gemini-2.0-flash-exp

# Modèle d'embedding (optionnel, par défaut: BAAI/bge-small-en-v1.5)
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5

# Flask Session Configuration (optionnel)
SESSION_TYPE=filesystem
SESSION_PERMANENT=false

